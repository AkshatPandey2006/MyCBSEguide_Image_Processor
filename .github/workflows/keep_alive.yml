name: Keep App Awake

on:
  schedule:
    - cron: '*/5 * * * *'  # This means: Run every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
    - name: Send request to keep the app awake
      run: |
        curl -I https://mycbseguide-image-processor.onrender.com/
